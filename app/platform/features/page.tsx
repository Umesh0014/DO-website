import IntelligenceLandingPage from "../../components/IntelligenceLandingPage";

export default function FeaturesPage() {
  return (
    <IntelligenceLandingPage
      activePage="service"
      heroEyebrow="How DataOrb works"
      heroHeading="From every conversation to the next best decision."
      heroBody="See how DataOrb turns interaction evidence into a shared operating record for service, revenue, and customer operations."
    />
  );
}
